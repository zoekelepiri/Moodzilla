<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="landscape"
        android:theme="@style/Theme.AndroidApp">
        <activity android:name=".MoodActivity"
            android:label="How you doin?"/>
        <activity android:name=".MoodActivityIntent"
            android:label="Quote for your feeling"/>
        <activity android:name=".About"
            android:label="About"/>
        <activity android:name=".TipsActivity"
            android:label="Tips"/>
        <activity android:name=".Task"
            android:label="My Tasks"/>
        <activity android:name=".MainActivity"
            android:label="Moodzilla"/>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>